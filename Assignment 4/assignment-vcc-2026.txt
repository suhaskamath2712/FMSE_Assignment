FMSE Assignment 4 (VCC)
-----------------------

Total Marks: 60
Due on Mon 23 March 2026.

Submit all your programming answers in separate files. The written
part of the answer to Q1 can be submitted by hard copy.


Q1. Consider the program and assertion below: [15 marks]

void main()
{
  int x = 0;
  int y = 0;
  while (x < 100)
  {
    x = x + 1;
    y = y + 2;
  }
  _(assert y >= 102)
}

(a) Your friend who is trying to prove the assertion comes up with the
invariant "x < y" for the while loop.  Write out the verification
conditions (VCs) entailed by this annotation. Which of them are
logically valid?

(b) Come up with your own invariant that is inductive and adequate to
prove the assertion. Use this to verify the program in VCC.


Q2. Consider the program below which is meant to model a library that
maintains a set of integers and supports operations like insert an
element into the set, and search for an element in the set. For
convenience the components of the data structure (like list, min, and
len) are declared as global variables. The given main() function uses
the library as a client. It inserts a set of non-negative numbers in
the set and then searches for a negative number. It finally asserts
that the search is unsuccessful.

Use VCC to prove the assertion in the client. You may make use
some invariants that the library data structure satisfies. Come up
with contracts for the library methods (listinit, listinsert, and
listsearch) and loop invariants in both the library methods and
client, which are adequate to prove the assertion in the client. The
library method contracts can be simple ones that are sufficient to
prove the correctness of the client. In particular, the contracts need
not describe the full functionality of the library. [Marks 25]


#define MAX 100

int list[MAX];
unsigned len;
int min;
// invariants:
// len <= MAX
// min <= A[0],...,A[len-1]
// ...

void listinit()
{
  len = 0;
  min = INT_MAX;
}

void listinsert(int val)
{
  list[len] = val;
  len++;
  if (len == 1)
    min = val;
  else if (val < min)
    min = val;
}  

int listsearch(int val)
{
  unsigned i = 0;

  while (i < len)
  {
    if (list[i] == val)
      return 1;
  }
  return 0;
}

int main()
{
  listinit();

  unsigned i = 0;
     
  while (i < MAX)
  {
    int n;  // n is uninitialized
    if (n >= 0)
      listinsert(n);
    i++;
  }
    
  int ret = listsearch(-13);
  _(assert ret == 0);
  return 0;
}


Q3. This question is similar to the midsem question which asked you to
verify the correctness of an integer counter which uses 2's complement
representation for integers. Assuming a width of 8 bits, the 2's
complement representation stores an unsigned 8-bit number "unum",
whose value ranges between 0 to 255, to represent a signed integer in
the range [-128,127]. A value of unum between 0 and 127 (inclusive)
represents a non-negative integer of the same magnitude, while a value
between 128 and 255 represents a negative integer whose magnitude is
the complement of unum in 256 (i.e. its magnitude is 256 - unum).

Below is the code of the concrete 2's complement based counter:

unsigned int unum;

void init()
{
  unum = 0;
}

void inc()
{
  if (unum == 255)
    unum = 0;
  else
    unum++;
}

void dec()
{
  ...
}

Complete the implementation of the decrement operation.  Write an
abstract implementation of the counter which uses a ghost \integer
variable called "intval". Give the combined functions for init, inc
and dec, along with invariants and gluing relation to phrase the
refinement conditions for the Rodin-style notion of refinement, and
verify them in VCC. It is enough to submit just the combined
functions and the annotations. [Marks 20]

