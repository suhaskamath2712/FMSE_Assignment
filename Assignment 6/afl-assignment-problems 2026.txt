AFL Assignment 2026

Before attempting the assignment, familiarize yourself with running AFL. Use
only the AFL version provided to you on the course web page (in
mafl-2.35b). Follow the instructions for running the AFL tools from the
README.txt on the course website.

Your solution to the assignment should be a single zip file named
<your-name>.zip. The zip file should contain the items mentioned below. 

1. First, produce a seed file using the following command:

     echo -n "xx" > seed

   IMPORTANT NOTE: You should use the last two digits of your IISc Student
   Registration number instead of "xx".

   The "echo -n" will ensure that the seed file will have two bytes
   exactly, with no trailing newline characters or null characters. (You
   should double check that this is true using "od -cb seed". It should
   display exactly two bytes.)

   a) Run the (modified) version of AFL that is provided to you on the course
   web page on the program for-class-while.c that is also provided to you
   on the class web page, using the seed file that you created above. You
   can kill AFL after it completes 20 cycles. You need to upload the entire 
   output folder as an output-fcw.zip file. (The output folder is the one that
   contains the sub-folders "queue", "hangs", etc.)
   
   b) List all test input file ids ("xxxxxx" in "id:xxxxxx" part of test input name)
   that were obtained using 1-bit flips, and for each such file, identify bit 
   position that is flipped compared to its parent ("yyyyyy" in "id:xxxxxx,src:yyyyyy"
   part of test input name). Take left most bit in the input char sequence as position
   zero. Write your answer in a file "problem1.pdf".

   Create a zip file problem1.zip file containing output-fcw.zip, problem1.pdf.
     
Marks: 10

2. Copy the following C program as-is.

#include<stdio.h>
#include<assert.h>

void main(int arc, char **argv){
char inp[5];
char *ptr =fgets(inp, sizeof inp, stdin);
 if(inp[0] == 'b'){
   if(inp[1] == 'a'){
     if(inp[2] == 'd'){
       if(inp[3] == '!'){
         assert(0);
       }
     }
   }
 }
return;
}


Run the (modified) version of AFL that is provided to you on the course
web page on the above using the seed file that contains only the single character "a".
Create this seed using the command described in problem 1. 

Run the AFL until it violates the assertion. 

Before running the AFL, run the following command.
export AFL_BENCH_UNTIL_CRASH=1
This command enables AFL to stop after finding the assertion violation.

Write all written answers asked below in a file named "problem2.pdf".

a) For each file in the "queue" and "crashes" folder, Print the file name and its contents 
(ignore README.txt file).
b) From the AFL screen write down the following field values:
i) No. cycles
ii) No. Levels
iii) No. of Generated test inputs
iv) No of inputs retained due to each of the fuzzing operations
In your answer, include a screenshot of AFL when it is terminated. Copy-paste this screen
shot in the same pdf file. Additionally, copy-paste the contents of the "fuzzer_stats" file
emitted by afl-fuzz into the same pdf file. Look at the output folder for this file.

c) From the run of afl-fuzz that you obtained as solution, draw the directed graph of 
input files in the "queue" folder. Each node in the graph is the test input id 
("xxxxxx" in "id:xxxxxx" part of test input name), whereas edge(s) is/are the
parent(s) of a test input file id.

d) In the same graph, add a special node as "crash" and add its parent edge(s).

e) Write your observation about this graph.
 
Marks: 20

3. Consider the test program crack-addr.c that is shared with this assignment.
For this problem, create a seed file exactly as in Problem 1.
 
a) Run afl-fuzz on this program using a 10-minute timeout. You need to upload 
the entire output folder into output-woa.zip. 

b) This program contains array overflows. Just before each array access statement
in program, write an assert statement to check whether array access   
that can catch array overflows. Submit this file as crack-addr-assert.c.

c) Identify inputs in the queue folder that cause assertion violations 
but are not in the crashes folder. For this part and next problem, write the 
answer in problem3.pdf. 

c) Briefly explain (in two lines) why those inputs that violate assertions
are not in the crashes folder (from the run mentioned in part a) above).

d) Run afl-fuzz on crack-addr-assert.c and share the output folder by uploading it
into output-assert.zip.

create a zip file problem3.zip file containing output-woa.zip, crack-addr-assert.c, 
output-assert.zip, and problem3.pdf.

Marks: 20

