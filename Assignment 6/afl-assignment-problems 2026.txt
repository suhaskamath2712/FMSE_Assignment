AFL Assignment 2026

Before attempting the assignment, familiarize yourself with running AFL. Use
only the AFL version provided to you on the course web page (in
mafl-2.35b). Follow the instructions for running the AFL tools from the
README.txt on the course website.

Your solution to the assignment should be a single zip file named
<your-name>.zip. The zip file should contain all the items mentioned below. 

1. First, produce a seed file using the following command:

     echo -n "xx" > seed

   IMPORTANT NOTE: You should use the first two letters of your IISc email ID
   instead of "xx".

   The "echo -n" will ensure that the seed file will have two bytes
   exactly, with no trailing newline characters or null characters. (You
   should double check that this is true using "od -cb seed". It should
   display exactly two bytes.)

   a) Run the (modified) version of AFL that is provided to you on the course
     web page on the program for-class-while.c that is also provided to you on
     the class web page, using the seed file that you created above. You can
     kill AFL after you see at least two files in the queue folder obtained
     using 1-bit or 2-bit flips, or you can kill after 10 minutes in case you
     still didn't obtain atleast two such files.  You need to upload the entire
     "output" folder as a file named output-fcw.zip.  (The output folder is the
     one that contains the sub-folders "queue", "hangs", etc.)
   
   b) List the IDs of all test input files that were obtained using 1-bit or
      2-bit flips. The ID of a file is the number that appears after "id:" in
      the file's name. For each such file, also identify the bit position(s)
      that is/are flipped compared to its parent. Take the leftmost bit in the
      input byte sequence as position zero. If there are more than two files in
      your queue that were obtained using 1- or 2-bit flips, identify the bit
      position(s) for any two files (you should clearly say which files you are
      considering for this).  Write your answer in a file "problem1.pdf".

     
Marks: 10

2. Copy the following C program as-is.

#include<stdio.h>
#include<assert.h>

void main(int arc, char **argv){
char inp[5];
char *ptr =fgets(inp, sizeof inp, stdin);
 if(inp[0] == 'b'){
   if(inp[1] == 'a'){
     if(inp[2] == 'd'){
       if(inp[3] == '!'){
         assert(0);
       }
     }
   }
 }
return;
}


Run the (modified) version of AFL that is provided to you on the course web
page on the above program using a seed file that contains only the single
character "a".  Create this seed using the command described in Problem 1.

Run AFL until it violates the assertion. 

Before running AFL, run the following command.
export AFL_BENCH_UNTIL_CRASH=1
This command enables AFL to stop after finding the assertion violation.

Place all written answers, figures, and screenshots asked for below within a file named "problem2.pdf".

a) For each file in the "queue" and "crashes" folder, write the file's name and its contents (ignore README.txt file). Regarding the contents, please show each byte as two digits in hexadecimal. 

b) From the AFL screen write down the following field values:
i) No. cycles
ii) No. Levels
iii) No. of Generated test inputs
iv) No of inputs retained due to each of the fuzzing operations

Include a screenshot of AFL when it is terminated. Additionally, copy-paste
the contents of the "fuzzer_stats" file emitted by afl-fuzz into the same file
problem2.pdf file. Look into the output folder for this file.

c) From the run of afl-fuzz that you obtained as solution, draw the
directed graph of input files in the "queue" folder. Each node in the graph
is a test input ID, whereas edges represent parent-child relationships. 

d) In the same graph, add a special node as "crash" and add its parent edge(s).

e) Write your observation about this graph.
 
Marks: 20

3. Consider the test program crack-addr.c that is shared with this assignment.
For this problem, create a seed file exactly as in Problem 1.
 
a) Run afl-fuzz on this program using a 10-minute timeout. You need to
   upload the entire output folder as a file named output-woa.zip.

b) This program contains array out of bounds issues. Just before each array
   access in program, write an assert statement to check whether that array
   access can go out of the bounds. Submit this modified program as
   crack-addr-assert.c.

c) Identify inputs in the queue folder that cause assertion violations but
   are not in the crashes folder. For this part and the next part, write
   your answer in a file named problem3.pdf.

d) Briefly explain (in two lines) why those inputs that violate assertions
   are not in the crashes folder (from the run mentioned in part-a above).

e) Run afl-fuzz on crack-addr-assert.c for 10 minutes and share the output
   folder by uploading it as a file named output-assert.zip.

Marks: 20

